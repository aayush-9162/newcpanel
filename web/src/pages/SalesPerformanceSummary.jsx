import SalesPerformance from './SalesPerformance.jsx';
// Same data shape as the SP page — kept as a separate route per the original cpanel.
export default function SalesPerformanceSummary() {
  return <SalesPerformance />;
}
