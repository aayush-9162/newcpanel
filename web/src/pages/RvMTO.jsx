import InvMTO from './InvMTO.jsx';
// RV-MTO uses the same structure as the Inv MTO view (shared inventory table).
// Kept as a separate route per the original cpanel layout.
export default function RvMTO() {
  return <InvMTO />;
}
